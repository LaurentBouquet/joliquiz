{% extends 'base.html.twig' %}

{% block title %}{{'User'|trans}} "{{ user.username }}"{% endblock %}

{% block body %}
    <h1>{{'User'|trans}} "{{ user.username }}"</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{{ 'Id'|trans }}</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>{{ 'Username'|trans }}</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>{{ 'Email'|trans }}</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>{{ 'Firstname'|trans }}</th>
                <td>{{ user.firstname }}</td>
            </tr>
            <tr>
                <th>{{ 'Lastname'|trans }}</th>
                <td>{{ user.lastname }}</td>
            </tr>
            <tr>
                <th>{{ 'Is active'|trans }}</th>
                <td>{{ user.isActive ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>{{ 'To receive result by email'|trans }}</th>
                <td>{{ user.toReceiveMyResultByEmail ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>{{ 'Group(s)'|trans }}</th>
                <td>{{ user.groups ? user.groups|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>{{ 'Role(s)'|trans }}</th>
                <td>{{ user.roles ? user.roles|join(', ') : '' }}</td>
            </tr>            
        </tbody>
    </table>


    {% if is_granted('ROLE_SUPER_ADMIN') %}
        <a class="btn btn-primary mr-2" href="{{ path('user_index') }}"><i class="fas fa-arrow-circle-left"></i> {{'Back to list'|trans}}</a>
        <a class="btn btn-info" href="{{ path('user_sendmail', {'id': user.id}) }}"><i class="fas fa-pencil-alt"></i> {{'Send confirmation mail'|trans}}</a>
        <a class="btn btn-primary" href="{{ path('user_edit', {'id': user.id}) }}"><i class="fas fa-pencil-alt"></i> {{'Edit'|trans}}</a>
    {% endif %}

{% endblock %}
