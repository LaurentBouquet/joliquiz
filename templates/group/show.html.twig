{% extends 'base.html.twig' %}

{% block title %}Group{% endblock %}

{% block body %}
    <h1>{{'Group'|trans}} "{{ group.name }}"</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{{ 'N°'|trans }}</th>
                <td>{{ group.id }}</td>
            </tr>
            <tr>
                <th>{{ 'Description'|trans }}</th>
                <td>{{ group.name }}</td>
            </tr>
            <tr>
                <th>{{ 'Code'|trans }}</th>
                <td>{{ group.code }}</td>
            </tr>
        </tbody>
    </table>

    {% if is_granted('ROLE_ADMIN') %}
        <a class="btn btn-secondary mr-2" href="{{ path('group_index') }}"><i class="fas fa-arrow-circle-left"></i> {{'Back to list'|trans}}</a>
        <a class="btn btn-primary" href="{{ path('group_edit', {'id': group.id}) }}"><i class="fas fa-pencil-alt"></i> {{'Edit'|trans}}</a>
    {% endif %}

{% endblock %}
