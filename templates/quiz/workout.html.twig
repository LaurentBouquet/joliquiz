{% extends 'base.html.twig' %}

{% block title %}Quiz{% endblock %}

{% block body %}
    <h1>Quiz "{{ quiz.title }}"</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        {% if is_granted('ROLE_USER') %}
            <a href="{{ path('quiz_workout', {'id': quiz.id}) }}">next</a>
        {% endif %}
    {{ form_end(form) }}

{% endblock %}
