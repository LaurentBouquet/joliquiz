{{ form_start(form) }}
    {{ form_widget(form) }}

    {% if is_granted('ROLE_SUPER_ADMIN') %}
        <a class="btn btn-secondary" href="{{ path('app_configuration_index') }}"><i class="fas fa-arrow-circle-left"></i> {{'Back to list'|trans}}</a>
        <button class="btn btn-primary"><i class="fas fa-check"></i> {{ button_label|default('Save'|trans) }}</button>
    {% endif %}
    
{{ form_end(form) }}
