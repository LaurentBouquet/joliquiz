{% extends 'base.html.twig' %}

{% block title %}Quiz index{% endblock %}

{% block body %}
    <h1>Quiz index</h1>

    {% if is_granted('ROLE_TEACHER') %}
        <div class="mb-3">
            <a class="btn btn-primary" href="{{ path('quiz_new') }}"><i class="fas fa-plus"></i> {{'Create new'|trans}}</a>
        </div>
    {% endif %}

    <table class="table">
        <thead>
            <tr>
                <th>Title</th>
                <th>Questions</th>
                <th>Categories</th>
                <th>Result after question</th>
                {% if is_granted('ROLE_TEACHER') %}
                    <th>Active</th>
                {% endif %}
                {% if is_granted('ROLE_USER') %}
                    <th>Actions</th>
                {% endif %}
            </tr>
        </thead>
        <tbody>
        {% for quiz in quizzes %}
            <tr>
                <td>{{ quiz.title }}</td>
                <td>{{ quiz.numberOfQuestions }}</td>
                <td>
                  {% for category in quiz.categories %}
                     - {{ category.longname }} <br>
                  {% endfor %}
                </td>
                <td>{{ quiz.showResultQuestion ? 'Yes' : 'No' }}</td>
                {% if is_granted('ROLE_TEACHER') %}
                    <td>{{ quiz.active ? 'Yes' : 'No' }}</td>
                    <td>
                        <div class="form-check-inline">
                            <a class="btn btn-primary btn-sm mr-2" href="{{ path('quiz_show', {'id': quiz.id}) }}"><i class="fas fa-eye"></i> {{'Show'|trans}}</a>
                            <a class="btn btn-primary btn-sm mr-2" href="{{ path('quiz_edit', {'id': quiz.id}) }}"><i class="fas fa-pencil-alt"></i> {{'Edit'|trans}}</a>
                            {{ include('quiz/_delete_form.html.twig') }}
                            <a class="btn btn-primary btn-sm mr-2" href="{{ path('quiz_start', {'id': quiz.id}) }}">{{'Start'|trans}} <i class="fas fa-arrow-circle-right"></i></a>
                        </div>
                    </td>
                {% else %}
                    {% if is_granted('ROLE_USER') %}
                    <td>
                        <a class="btn btn-primary btn-sm mr-2" href="{{ path('quiz_start', {'id': quiz.id}) }}">{{'Start'|trans}} <i class="fas fa-arrow-circle-right"></i></a>
                    </td>
                    {% endif %}
                {% endif %}
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
